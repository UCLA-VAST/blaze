package blaze;

option java_package = "org.apache.hadoop.yarn.fcs";

message Gam2NamRequest {
  enum MsgType {
    ACCNAMES = 0;  
    SHARERATE = 1;  
  }
  required MsgType type = 1;
  optional bool pull = 2;
}

message Nam2GamAccNames {
  optional bool updated = 1;
  repeated string acc_names = 2;
}
