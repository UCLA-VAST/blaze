0. Ensure correct path to the FCS license is configured
   # export LM_LICENSE_FILE=port@host:$LM_LICENSE_FILE

1. Compile example spark application
- ensure Apache Maven is installed
- run `mvn package` in $ROOTDIR/examples/$EXAMPLE/app folder
- use `run.sh` to validate application using local Spark on CPU

2. Setup example accelerators
- Compile accelerator task 
  run `make` in $ROOTDIR/examples/$EXAMPLE/acc_task (if *.so file does not exist)
- run the following commands in $ROOTDIR/examples/$EXAMPLE/acc_task folder:
  # $ROOTDIR/bin/fcs-setup-example

3. Start node manager locally
- run the following command:
  # $ROOTDIR/nam/sbin/start_nam.sh
- to run examples with FPGA accelerator, please ensure libxilinxopencl.so
  from Xilinx SDAccel is in LD_LIBRARY_PATH search paths
- check log file to see if node manager is successfully launched: 
  # cat $ROOTDIR/nam/logs/falcon_runtime_nodemanager_$USER_$HOSTNAME.log

4. Run example Spark application
  # cd $ROOTDIR/examples/logisticRegression/app 
  # ./run.sh <option list>

5. Stop node manager:
  # $ROOTDIR/nam/sbin/stop_nam.sh
